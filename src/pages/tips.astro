---
import BaseLayout from '~/layouts/BaseLayout.astro';
import CopyButton from '~/components/CopyButton.astro';

const base = import.meta.env.BASE_URL.replace(/\/$/, '');
const currentHref = `${base}/tips/`;

// ── 검증된 명령 (2026-08-19 실측) ──
const cmdNodeCheck = `node -v    # v22 이상이면 OK. 아무 출력이 없으면 아래로`;
const cmdNodeInstall = `# macOS (nvm 권장):
curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.40.1/install.sh | bash
# 터미널 재시작 후:
nvm install 22 && nvm use 22

# Windows: https://nodejs.org 에서 LTS 인스톨러 설치`;
const cmdInstall = `curl -fsSL https://opencode.ai/install | bash`;
const cmdInstallNpm = `npm install -g opencode-ai@latest   # Windows는 이쪽`;
const cmdVerify = `opencode --version`;
const cmdOmoInstall = `bunx oh-my-opencode-slim@latest install

# Bun이 없으면 npx도 같다:
npx oh-my-opencode-slim@latest install`;
const cmdAuth = `opencode auth login    # 목록에서 zai-coding-plan 선택 → 키 붙여넣기`;
const cmdModels = `opencode models --refresh    # 내 구독으로 열리는 모델 새로고침`;
const cmdPresetOpen = `# macOS / Linux
open ~/.config/opencode/oh-my-opencode-slim.json

# 또는 에디터로
code ~/.config/opencode/oh-my-opencode-slim.json`;
const cmdPreset = `{
  "$schema": "https://unpkg.com/oh-my-opencode-slim@latest/oh-my-opencode-slim.schema.json",
  "preset": "glm",
  "presets": {
    "glm": {
      "orchestrator": { "model": "zai-coding-plan/glm-5.3", "skills": ["*"], "mcps": ["*"] },
      "oracle":       { "model": "zai-coding-plan/glm-5.3", "skills": [], "mcps": [] },
      "librarian":    { "model": "zai-coding-plan/glm-5-turbo", "skills": [], "mcps": ["context7"] },
      "explorer":     { "model": "zai-coding-plan/glm-5-turbo", "skills": [], "mcps": [] },
      "designer":     { "model": "zai-coding-plan/glm-5.3", "skills": [], "mcps": [] },
      "fixer":        { "model": "zai-coding-plan/glm-5.3", "skills": [], "mcps": [] }
    }
  }
}`;
const cmdRun = `cd ~/my-project     # 작업할 폴더로 이동
opencode            # 실행`;
const cmdFirst = `이 폴더의 코드를 읽고 프로젝트 구조를 요약해줘`;
const cmdVisionInstall = `git clone https://github.com/cskwork/promptbox ~/promptbox
ln -s ~/promptbox/plugins/glm-vision ~/.agents/skills/glm-vision`;
const cmdVisionUse = `python3 ~/.agents/skills/glm-vision/glm_vision.py 이미지.png -q "이 이미지의 오류 메시지를 그대로 읽어줘"`;
const cmdPrimeInstall = `curl -fsSL https://app.primeintellect.ai/prime-agent/install.sh | sh
# 또는: npm install -g prime-agent   (Node 22.8 이상)

cd ~/my-project && prime-agent
# 에이전트 안에서: /login → ZAI 선택 → 키 붙여넣기`;
---

<BaseLayout
  title="Tips & Tricks — promptbox"
  titleKo="Tips & Tricks — promptbox"
  description="Zero-to-hero beginner tutorial in Korean: get your Z.ai GLM key, install OpenCode and oh-my-opencode-slim, wire up GLM-5.3, and run your first agent team session."
  descriptionKo="완전 입문자용 튜토리얼: Z.ai GLM API 키 발급부터 OpenCode + oh-my-opencode-slim 설치, GLM-5.3 연결, 첫 세션까지 단계별로."
  currentHref={currentHref}
>
  <nav class="mb-4 flex items-center gap-1.5 text-sm text-ink-500" aria-label="Breadcrumb" data-aria-en="Breadcrumb" data-aria-ko="경로">
    <a href={base + '/'} class="hover:text-accent-700 dark:hover:text-accent-400"><span data-lang="en">Home</span><span data-lang="ko">홈</span></a>
    <span aria-hidden="true">›</span>
    <span class="text-ink-700 dark:text-ink-200">Tips &amp; Tricks</span>
  </nav>

  <header class="glass-panel mb-6 rounded-2xl p-5 lg:p-6">
    <h1 class="text-2xl font-bold tracking-tight text-ink-900 dark:text-ink-50 lg:text-3xl">
      <span data-lang="en">Tips &amp; Tricks — zero to coding agent</span>
      <span data-lang="ko">Tips &amp; Tricks — 코딩 에이전트 0에서 시작하기</span>
    </h1>
    <p class="mt-2 text-pretty leading-relaxed text-ink-600 dark:text-ink-300">
      <span data-lang="en">A step-by-step beginner tutorial: receive a Z.ai GLM key, install OpenCode, add the oh-my-opencode-slim agent team, point it at GLM-5.3, and run your first session. Every command was verified on a real machine (2026-08-19).</span>
      <span data-lang="ko">Z.ai GLM 키를 받는 순간부터 OpenCode를 설치하고, 에이전트 팀 플러그인(oh-my-opencode-slim)을 얹고, GLM-5.3에 연결해 첫 세션을 돌리기까지. 코딩 지식 없이 따라 할 수 있는 단계별 튜토리얼이며, 아래 모든 명령은 실제 머신에서 검증했다(2026-08-19).</span>
    </p>
    <p class="mt-3 rounded-lg border border-accent-500/30 bg-accent-500/10 px-3 py-2 text-sm text-ink-700 dark:text-ink-200">
      <span data-lang="en"><strong class="font-semibold">Default setup</strong>: <a class="text-accent-700 dark:text-accent-400 font-medium underline underline-offset-2" href={`${base}/harnesses/opencode/`}>OpenCode</a> + <a class="text-accent-700 dark:text-accent-400 font-medium underline underline-offset-2" href={`${base}/harnesses/oh-my-opencode-slim/`}>oh-my-opencode-slim</a> + <code class="font-mono text-[0.85em]">glm-5.3</code>. Prefer a single-agent harness? Prime Agent is at the bottom of this page.</span>
      <span data-lang="ko"><strong class="font-semibold">기본 구성</strong>은 <a class="text-accent-700 dark:text-accent-400 font-medium underline underline-offset-2" href={`${base}/harnesses/opencode/`}>OpenCode</a> + <a class="text-accent-700 dark:text-accent-400 font-medium underline underline-offset-2" href={`${base}/harnesses/oh-my-opencode-slim/`}>oh-my-opencode-slim</a> + <code class="font-mono text-[0.85em]">glm-5.3</code>이다. 에이전트 한 명짜리 단순한 구성을 원하면 이 페이지 맨 아래 Prime Agent를 보라.</span>
    </p>
  </header>

  <div class="space-y-6">

    <!-- STEP 0 -->
    <section class="glass-panel rounded-2xl p-5 lg:p-6">
      <div class="mb-2 flex items-baseline gap-2">
        <span class="font-mono text-sm font-bold text-accent-700 dark:text-accent-400">STEP 0</span>
        <h2 class="text-lg font-semibold text-ink-900 dark:text-ink-50"><span data-lang="en">Prepare a Z.ai API key</span><span data-lang="ko">준비물 — Z.ai API 키</span></h2>
      </div>
      <p class="text-sm leading-relaxed text-ink-600 dark:text-ink-300">
        <span data-lang="en">You need one Z.ai GLM key. Two ways to get it:</span>
        <span data-lang="ko">필요한 것은 딱 하나, Z.ai GLM 키다. 받는 방법은 두 가지다:</span>
      </p>
      <ul class="mt-2 list-disc space-y-1.5 pl-6 text-sm leading-relaxed text-ink-600 dark:text-ink-300 marker:text-accent-500">
        <li>
          <span data-lang="en"><strong class="font-semibold text-ink-800 dark:text-ink-100">Shared key from this console (recommended)</strong>: log in to the <a class="text-accent-700 dark:text-accent-400 font-medium underline underline-offset-2" href="https://zai-coding-plan.vercel.app" target="_blank" rel="noopener">ZAI Plan Console</a> → 「API 키 금고」 → tap 「관리자에게 키 열람 요청」. Once the admin approves, the key appears in the vault — press 열람 to copy it.</span>
          <span data-lang="ko"><strong class="font-semibold text-ink-800 dark:text-ink-100">이 콘솔에서 공유받기(권장)</strong>: <a class="text-accent-700 dark:text-accent-400 font-medium underline underline-offset-2" href="https://zai-coding-plan.vercel.app" target="_blank" rel="noopener">ZAI Plan Console</a>에 Google 로그인 → 「API 키 금고」 화면 → 「관리자에게 키 열람 요청」 버튼 누르기. 관리자가 승인하면 금고에 공유 키가 나타나고, 「열람」 버튼으로 복사할 수 있다.</span>
        </li>
        <li>
          <span data-lang="en"><strong class="font-semibold text-ink-800 dark:text-ink-100">Your own subscription</strong>: subscribe to the GLM Coding Plan at <a class="text-accent-700 dark:text-accent-400 font-medium underline underline-offset-2" href="https://z.ai" target="_blank" rel="noopener">z.ai</a> and issue an API key from your dashboard.</span>
          <span data-lang="ko"><strong class="font-semibold text-ink-800 dark:text-ink-100">직접 구독</strong>: <a class="text-accent-700 dark:text-accent-400 font-medium underline underline-offset-2" href="https://z.ai" target="_blank" rel="noopener">z.ai</a>에서 GLM Coding Plan을 구독하고, 대시보드에서 API 키를 발급받는다.</span>
        </li>
      </ul>
      <p class="mt-2 rounded-lg border border-amber-500/30 bg-amber-500/10 px-3 py-2 text-sm text-amber-800 dark:text-amber-200">
        <span data-lang="en">⚠ Treat the key like a password. Never paste it in chats or screenshots. If it leaks, ask for rotation immediately.</span>
        <span data-lang="ko">⚠ 키는 비밀번호다. 채팅·스크린샷에 절대 붙여넣지 말고, 유출되면 즉시 관리자에게 회전(교체)을 요청하라.</span>
      </p>
    </section>

    <!-- STEP 1 -->
    <section class="glass-panel rounded-2xl p-5 lg:p-6">
      <div class="mb-2 flex items-baseline gap-2">
        <span class="font-mono text-sm font-bold text-accent-700 dark:text-accent-400">STEP 1</span>
        <h2 class="text-lg font-semibold text-ink-900 dark:text-ink-50"><span data-lang="en">Open the terminal</span><span data-lang="ko">터미널 열기</span></h2>
      </div>
      <p class="text-sm leading-relaxed text-ink-600 dark:text-ink-300">
        <span data-lang="en">macOS: press <kbd class="rounded border border-ink-300 px-1 font-mono text-xs dark:border-ink-700">⌘+Space</kbd>, type <code class="font-mono text-[0.85em]">터미널</code> (or Terminal), Enter. Windows: press the Start key, type <code class="font-mono text-[0.85em]">PowerShell</code>, Enter.</span>
        <span data-lang="ko">macOS: <kbd class="rounded border border-ink-300 px-1 font-mono text-xs dark:border-ink-700">⌘+스페이스</kbd>를 누르고 <code class="font-mono text-[0.85em]">터미널</code>(또는 Terminal) 입력 후 엔터. Windows: 시작 버튼 → <code class="font-mono text-[0.85em]">PowerShell</code> 검색 → 엔터. 이후 모든 명령은 이 창에 입력한다.</span>
      </p>
    </section>

    <!-- STEP 2 -->
    <section class="glass-panel rounded-2xl p-5 lg:p-6">
      <div class="mb-2 flex items-baseline gap-2">
        <span class="font-mono text-sm font-bold text-accent-700 dark:text-accent-400">STEP 2</span>
        <h2 class="text-lg font-semibold text-ink-900 dark:text-ink-50"><span data-lang="en">Check Node.js (v22+)</span><span data-lang="ko">Node.js 확인 (v22 이상)</span></h2>
      </div>
      <p class="mb-3 text-sm leading-relaxed text-ink-600 dark:text-ink-300">
        <span data-lang="en">OpenCode itself ships as a binary, but the plugin installer in STEP 4 runs through <code class="font-mono text-[0.85em]">npx</code>/<code class="font-mono text-[0.85em]">bunx</code>. Check Node first:</span>
        <span data-lang="ko">OpenCode 자체는 실행 파일로 설치되지만, STEP 4의 플러그인 설치기가 <code class="font-mono text-[0.85em]">npx</code>/<code class="font-mono text-[0.85em]">bunx</code>로 돌아간다. 먼저 확인:</span>
      </p>
      <div class="mb-3 flex items-stretch gap-2">
        <code id="tip-node-check" class="min-w-0 flex-1 overflow-x-auto whitespace-pre rounded-lg border border-ink-800/70 bg-ink-950 px-3 py-2 font-mono text-[0.82rem] text-ink-100">{cmdNodeCheck}</code>
        <CopyButton target="#tip-node-check" labelEn="Copy" labelKo="복사" size="sm" variant="ghost" class="shrink-0" />
      </div>
      <p class="mb-2 text-sm leading-relaxed text-ink-600 dark:text-ink-300">
        <span data-lang="en">If the version is older or the command is not found, install Node 22:</span>
        <span data-lang="ko">버전이 더 낮거나 명령이 없다고 나오면 Node 22을 설치한다:</span>
      </p>
      <div class="flex items-stretch gap-2">
        <code id="tip-node-install" class="min-w-0 flex-1 overflow-x-auto whitespace-pre rounded-lg border border-ink-800/70 bg-ink-950 px-3 py-2 font-mono text-[0.82rem] text-ink-100">{cmdNodeInstall}</code>
        <CopyButton target="#tip-node-install" labelEn="Copy" labelKo="복사" size="sm" variant="ghost" class="shrink-0" />
      </div>
    </section>

    <!-- STEP 3 -->
    <section class="glass-panel rounded-2xl p-5 lg:p-6">
      <div class="mb-2 flex items-baseline gap-2">
        <span class="font-mono text-sm font-bold text-accent-700 dark:text-accent-400">STEP 3</span>
        <h2 class="text-lg font-semibold text-ink-900 dark:text-ink-50"><span data-lang="en">Install OpenCode</span><span data-lang="ko">OpenCode 설치</span></h2>
      </div>
      <p class="mb-2 text-sm leading-relaxed text-ink-600 dark:text-ink-300">
        <span data-lang="en">One line (macOS/Linux/WSL) — the official installer. The command it installs is <code class="font-mono text-[0.85em]">opencode</code>:</span>
        <span data-lang="ko">한 줄이면 끝난다(macOS/Linux/WSL 공식 인스톨러). 설치되는 명령 이름은 <code class="font-mono text-[0.85em]">opencode</code>다:</span>
      </p>
      <div class="mb-3 flex items-stretch gap-2">
        <code id="tip-install" class="min-w-0 flex-1 overflow-x-auto whitespace-pre rounded-lg border border-ink-800/70 bg-ink-950 px-3 py-2 font-mono text-[0.82rem] text-ink-100">{cmdInstall}</code>
        <CopyButton target="#tip-install" labelEn="Copy" labelKo="복사" size="sm" variant="ghost" class="shrink-0" />
      </div>
      <p class="mb-2 text-sm leading-relaxed text-ink-600 dark:text-ink-300">
        <span data-lang="en">On Windows, or if you prefer npm — note the package name is <code class="font-mono text-[0.85em]">opencode-ai</code>, not <code class="font-mono text-[0.85em]">opencode</code> (verified: v1.18.18):</span>
        <span data-lang="ko">Windows이거나 npm이 익숙하면 이쪽. 패키지 이름이 <code class="font-mono text-[0.85em]">opencode</code>가 아니라 <code class="font-mono text-[0.85em]">opencode-ai</code>인 점에 주의(실측 v1.18.18):</span>
      </p>
      <div class="mb-3 flex items-stretch gap-2">
        <code id="tip-install-npm" class="min-w-0 flex-1 overflow-x-auto whitespace-pre rounded-lg border border-ink-800/70 bg-ink-950 px-3 py-2 font-mono text-[0.82rem] text-ink-100">{cmdInstallNpm}</code>
        <CopyButton target="#tip-install-npm" labelEn="Copy" labelKo="복사" size="sm" variant="ghost" class="shrink-0" />
      </div>
      <div class="flex items-stretch gap-2">
        <code id="tip-verify" class="min-w-0 flex-1 overflow-x-auto whitespace-pre rounded-lg border border-ink-800/70 bg-ink-950 px-3 py-2 font-mono text-[0.82rem] text-ink-100">{cmdVerify}</code>
        <CopyButton target="#tip-verify" labelEn="Copy" labelKo="복사" size="sm" variant="ghost" class="shrink-0" />
      </div>
    </section>

    <!-- STEP 4 -->
    <section class="glass-panel rounded-2xl p-5 lg:p-6">
      <div class="mb-2 flex items-baseline gap-2">
        <span class="font-mono text-sm font-bold text-accent-700 dark:text-accent-400">STEP 4</span>
        <h2 class="text-lg font-semibold text-ink-900 dark:text-ink-50"><span data-lang="en">Add the agent team (oh-my-opencode-slim)</span><span data-lang="ko">에이전트 팀 얹기 (oh-my-opencode-slim)</span></h2>
      </div>
      <p class="mb-3 text-sm leading-relaxed text-ink-600 dark:text-ink-300">
        <span data-lang="en">This plugin turns one agent into a team of seven: an Orchestrator that plans and dispatches, plus Explorer (search), Oracle (deep review), Librarian (docs), Designer (UI), Fixer (implementation), and Council (multi-model vote). Specialists run as background tasks in parallel.</span>
        <span data-lang="ko">이 플러그인은 에이전트 한 명을 일곱 명 팀으로 바꾼다. 계획하고 일을 나눠주는 Orchestrator(지휘자) 아래에 Explorer(코드 탐색), Oracle(깊은 검토), Librarian(문서 조회), Designer(UI), Fixer(구현), Council(여러 모델 동시 판단)이 붙는다. 전문 에이전트는 background task(뒤에서 동시에 도는 작업)로 병렬 실행된다.</span>
      </p>
      <div class="mb-3 flex items-stretch gap-2">
        <code id="tip-omo-install" class="min-w-0 flex-1 overflow-x-auto whitespace-pre rounded-lg border border-ink-800/70 bg-ink-950 px-3 py-2 font-mono text-[0.82rem] text-ink-100">{cmdOmoInstall}</code>
        <CopyButton target="#tip-omo-install" labelEn="Copy" labelKo="복사" size="sm" variant="ghost" class="shrink-0" />
      </div>
      <p class="text-sm leading-relaxed text-ink-600 dark:text-ink-300">
        <span data-lang="en">The installer registers the plugin in <code class="font-mono text-[0.85em]">~/.config/opencode/opencode.json</code> and creates <code class="font-mono text-[0.85em]">~/.config/opencode/oh-my-opencode-slim.json</code>. It only generates <code class="font-mono text-[0.85em]">openai</code> and <code class="font-mono text-[0.85em]">opencode-go</code> presets — STEP 6 replaces that with a GLM one.</span>
        <span data-lang="ko">설치기가 <code class="font-mono text-[0.85em]">~/.config/opencode/opencode.json</code>에 플러그인을 등록하고 <code class="font-mono text-[0.85em]">~/.config/opencode/oh-my-opencode-slim.json</code>을 만든다. 다만 기본 생성되는 프리셋은 <code class="font-mono text-[0.85em]">openai</code>와 <code class="font-mono text-[0.85em]">opencode-go</code> 둘뿐이라, GLM으로 바꾸는 일은 STEP 6에서 한다.</span>
      </p>
    </section>

    <!-- STEP 5 -->
    <section class="glass-panel rounded-2xl p-5 lg:p-6">
      <div class="mb-2 flex items-baseline gap-2">
        <span class="font-mono text-sm font-bold text-accent-700 dark:text-accent-400">STEP 5</span>
        <h2 class="text-lg font-semibold text-ink-900 dark:text-ink-50"><span data-lang="en">Connect your Z.ai key</span><span data-lang="ko">Z.ai 키 연결</span></h2>
      </div>
      <p class="mb-3 text-sm leading-relaxed text-ink-600 dark:text-ink-300">
        <span data-lang="en">Run the login command, pick <strong>zai-coding-plan</strong> from the provider list, and paste the key. It is stored locally in <code class="font-mono text-[0.85em]">~/.local/share/opencode/auth.json</code> and reused automatically — you only do this once.</span>
        <span data-lang="ko">로그인 명령을 실행하고 공급자 목록에서 <strong>zai-coding-plan</strong>을 고른 뒤 키를 붙여넣는다. 키는 내 컴퓨터의 <code class="font-mono text-[0.85em]">~/.local/share/opencode/auth.json</code>에만 저장되고 다음부터 자동으로 쓰인다. 한 번만 하면 된다.</span>
      </p>
      <div class="mb-3 flex items-stretch gap-2">
        <code id="tip-auth" class="min-w-0 flex-1 overflow-x-auto whitespace-pre rounded-lg border border-ink-800/70 bg-ink-950 px-3 py-2 font-mono text-[0.82rem] text-ink-100">{cmdAuth}</code>
        <CopyButton target="#tip-auth" labelEn="Copy" labelKo="복사" size="sm" variant="ghost" class="shrink-0" />
      </div>
      <p class="mb-2 text-sm leading-relaxed text-ink-600 dark:text-ink-300">
        <span data-lang="en">Then refresh the model catalog so OpenCode sees what your subscription unlocks:</span>
        <span data-lang="ko">그다음 모델 목록을 새로고침해야 내 구독으로 열리는 모델이 보인다:</span>
      </p>
      <div class="flex items-stretch gap-2">
        <code id="tip-models" class="min-w-0 flex-1 overflow-x-auto whitespace-pre rounded-lg border border-ink-800/70 bg-ink-950 px-3 py-2 font-mono text-[0.82rem] text-ink-100">{cmdModels}</code>
        <CopyButton target="#tip-models" labelEn="Copy" labelKo="복사" size="sm" variant="ghost" class="shrink-0" />
      </div>
    </section>

    <!-- STEP 6 -->
    <section class="glass-panel rounded-2xl p-5 lg:p-6">
      <div class="mb-2 flex items-baseline gap-2">
        <span class="font-mono text-sm font-bold text-accent-700 dark:text-accent-400">STEP 6</span>
        <h2 class="text-lg font-semibold text-ink-900 dark:text-ink-50"><span data-lang="en">Point the team at GLM-5.3</span><span data-lang="ko">팀 전체를 GLM-5.3에 물리기</span></h2>
      </div>
      <p class="mb-3 text-sm leading-relaxed text-ink-600 dark:text-ink-300">
        <span data-lang="en">Open the plugin config:</span>
        <span data-lang="ko">플러그인 설정 파일을 연다:</span>
      </p>
      <div class="mb-3 flex items-stretch gap-2">
        <code id="tip-preset-open" class="min-w-0 flex-1 overflow-x-auto whitespace-pre rounded-lg border border-ink-800/70 bg-ink-950 px-3 py-2 font-mono text-[0.82rem] text-ink-100">{cmdPresetOpen}</code>
        <CopyButton target="#tip-preset-open" labelEn="Copy" labelKo="복사" size="sm" variant="ghost" class="shrink-0" />
      </div>
      <p class="mb-3 text-sm leading-relaxed text-ink-600 dark:text-ink-300">
        <span data-lang="en">Replace the contents with this. Heavy thinking goes to <code class="font-mono text-[0.85em]">glm-5.3</code> (1,000,000-token context, released 2026-08-14); the high-volume search and docs lanes go to the cheaper <code class="font-mono text-[0.85em]">glm-5-turbo</code>. This exact JSON validates against the plugin's own schema.</span>
        <span data-lang="ko">내용을 아래로 바꾼다. 무겁게 판단해야 하는 자리는 <code class="font-mono text-[0.85em]">glm-5.3</code>(컨텍스트 100만 토큰, 2026-08-14 공개), 양이 많고 단순한 탐색·문서 조회 자리는 값싼 <code class="font-mono text-[0.85em]">glm-5-turbo</code>로 나눴다. 아래 JSON은 플러그인이 배포하는 스키마로 검증을 통과했다.</span>
      </p>
      <div class="mb-3 flex items-stretch gap-2">
        <code id="tip-preset" class="min-w-0 flex-1 overflow-x-auto whitespace-pre rounded-lg border border-ink-800/70 bg-ink-950 px-3 py-2 font-mono text-[0.82rem] text-ink-100">{cmdPreset}</code>
        <CopyButton target="#tip-preset" labelEn="Copy" labelKo="복사" size="sm" variant="ghost" class="shrink-0" />
      </div>
      <p class="text-sm leading-relaxed text-ink-600 dark:text-ink-300">
        <span data-lang="en">Save, restart <code class="font-mono text-[0.85em]">opencode</code>, and the whole team runs on GLM. To swap presets mid-session, type <code class="font-mono text-[0.85em]">/preset</code>.</span>
        <span data-lang="ko">저장하고 <code class="font-mono text-[0.85em]">opencode</code>를 다시 실행하면 팀 전체가 GLM 위에서 돈다. 세션 도중 프리셋을 갈아끼우려면 <code class="font-mono text-[0.85em]">/preset</code>.</span>
      </p>
    </section>

    <!-- STEP 7 -->
    <section class="glass-panel rounded-2xl p-5 lg:p-6">
      <div class="mb-2 flex items-baseline gap-2">
        <span class="font-mono text-sm font-bold text-accent-700 dark:text-accent-400">STEP 7</span>
        <h2 class="text-lg font-semibold text-ink-900 dark:text-ink-50"><span data-lang="en">Run it and talk</span><span data-lang="ko">실행하고 첫 대화</span></h2>
      </div>
      <div class="mb-3 flex items-stretch gap-2">
        <code id="tip-run" class="min-w-0 flex-1 overflow-x-auto whitespace-pre rounded-lg border border-ink-800/70 bg-ink-950 px-3 py-2 font-mono text-[0.82rem] text-ink-100">{cmdRun}</code>
        <CopyButton target="#tip-run" labelEn="Copy" labelKo="복사" size="sm" variant="ghost" class="shrink-0" />
      </div>
      <p class="mb-3 text-sm leading-relaxed text-ink-600 dark:text-ink-300">
        <span data-lang="en">You are talking to the Orchestrator. Just write what you want, in Korean:</span>
        <span data-lang="ko">대화 상대는 Orchestrator다. 하고 싶은 일을 한국어로 그대로 쓰면 된다:</span>
      </p>
      <div class="mb-3 flex items-stretch gap-2">
        <code id="tip-first" class="min-w-0 flex-1 overflow-x-auto whitespace-pre rounded-lg border border-ink-800/70 bg-ink-950 px-3 py-2 font-mono text-[0.82rem] text-ink-100">{cmdFirst}</code>
        <CopyButton target="#tip-first" labelEn="Copy" labelKo="복사" size="sm" variant="ghost" class="shrink-0" />
      </div>
      <ul class="list-disc space-y-1.5 pl-6 text-sm leading-relaxed text-ink-600 dark:text-ink-300 marker:text-accent-500">
        <li>
          <span data-lang="en"><code class="font-mono text-[0.85em]">/models</code> — switch model. <code class="font-mono text-[0.85em]">/preset</code> — switch the whole team's models at once.</span>
          <span data-lang="ko"><code class="font-mono text-[0.85em]">/models</code> — 모델 변경. <code class="font-mono text-[0.85em]">/preset</code> — 팀 전체 모델을 한 번에 교체.</span>
        </li>
        <li>
          <span data-lang="en"><code class="font-mono text-[0.85em]">@explorer 이 저장소에서 인증 로직 찾아줘</code> — call a specialist directly instead of waiting for the Orchestrator to delegate.</span>
          <span data-lang="ko"><code class="font-mono text-[0.85em]">@explorer 이 저장소에서 인증 로직 찾아줘</code> — 지휘자를 거치지 않고 전문 에이전트를 직접 부를 수 있다.</span>
        </li>
        <li>
          <span data-lang="en">Put an <code class="font-mono text-[0.85em]">AGENTS.md</code> in the project root and OpenCode loads it at startup as your standing project instructions.</span>
          <span data-lang="ko">프로젝트 루트에 <code class="font-mono text-[0.85em]">AGENTS.md</code>를 두면 시작할 때 자동으로 읽어 프로젝트 상시 지시사항으로 삼는다.</span>
        </li>
      </ul>
      <p class="mt-3 rounded-lg border border-amber-500/30 bg-amber-500/10 px-3 py-2 text-sm text-amber-800 dark:text-amber-200">
        <span data-lang="en">⚠ GLM Coding Plan models cannot read images — all five of them declare no image input. Pasting a screenshot will not work; use glm-vision in STEP 8 instead.</span>
        <span data-lang="ko">⚠ GLM Coding Plan 모델은 이미지를 읽지 못한다 — 5개 모델 모두 이미지 입력을 지원하지 않는다고 선언한다. 스크린샷을 붙여넣어도 소용없으니, 이미지는 STEP 8의 glm-vision으로 처리하라.</span>
      </p>
    </section>

    <!-- STEP 8 -->
    <section class="glass-panel rounded-2xl p-5 lg:p-6">
      <div class="mb-2 flex items-baseline gap-2">
        <span class="font-mono text-sm font-bold text-accent-700 dark:text-accent-400">STEP 8</span>
        <h2 class="text-lg font-semibold text-ink-900 dark:text-ink-50"><span data-lang="en">(Optional) Skill kit &amp; image reading</span><span data-lang="ko">(선택) 스킬 키트와 이미지 읽기</span></h2>
      </div>
      <p class="mb-4 text-sm leading-relaxed text-ink-600 dark:text-ink-300">
        <span data-lang="en">Copy the <a class="text-accent-700 dark:text-accent-400 font-medium underline underline-offset-2" href={`${base}/prompts/agents-quick-onboarding/`}>quick-onboarding prompt</a> from this site and paste it to the agent. It sets up the shared <code class="font-mono text-[0.85em]">~/.agents/</code> layout and symlinks every skill into each CLI you own — OpenCode reads them from <code class="font-mono text-[0.85em]">~/.config/opencode/skills/</code>.</span>
        <span data-lang="ko">이 사이트의 <a class="text-accent-700 dark:text-accent-400 font-medium underline underline-offset-2" href={`${base}/prompts/agents-quick-onboarding/`}>퀵 온보딩 프롬프트</a>를 복사해 에이전트에 그대로 붙여넣으면, 공용 <code class="font-mono text-[0.85em]">~/.agents/</code> 구조를 잡고 보유한 모든 CLI에 스킬을 심링크해 준다. OpenCode는 <code class="font-mono text-[0.85em]">~/.config/opencode/skills/</code>에서 스킬을 읽는다.</span>
      </p>
      <p class="mb-3 text-sm leading-relaxed text-ink-600 dark:text-ink-300">
        <span data-lang="en">The <a class="text-accent-700 dark:text-accent-400 font-medium underline underline-offset-2" href={`${base}/plugins/glm-vision/`}>glm-vision plugin</a> covers the blind spot from STEP 7: it sends any image to GLM-4.6V and returns the exact text/tables it reads. Two lines to install:</span>
        <span data-lang="ko">STEP 7에서 짚은 사각지대를 <a class="text-accent-700 dark:text-accent-400 font-medium underline underline-offset-2" href={`${base}/plugins/glm-vision/`}>glm-vision 플러그인</a>이 메운다. 이미지를 GLM-4.6V로 보내 글자·표를 그대로 읽어 돌려준다. 설치는 두 줄:</span>
      </p>
      <div class="mb-3 flex items-stretch gap-2">
        <code id="tip-vision-install" class="min-w-0 flex-1 overflow-x-auto whitespace-pre rounded-lg border border-ink-800/70 bg-ink-950 px-3 py-2 font-mono text-[0.82rem] text-ink-100">{cmdVisionInstall}</code>
        <CopyButton target="#tip-vision-install" labelEn="Copy" labelKo="복사" size="sm" variant="ghost" class="shrink-0" />
      </div>
      <div class="mb-3 flex items-stretch gap-2">
        <code id="tip-vision-use" class="min-w-0 flex-1 overflow-x-auto whitespace-pre rounded-lg border border-ink-800/70 bg-ink-950 px-3 py-2 font-mono text-[0.82rem] text-ink-100">{cmdVisionUse}</code>
        <CopyButton target="#tip-vision-use" labelEn="Copy" labelKo="복사" size="sm" variant="ghost" class="shrink-0" />
      </div>
      <p class="text-sm leading-relaxed text-ink-600 dark:text-ink-300">
        <span data-lang="en">End-to-end test passed against the real glm-4.6v API (2026-08-18): it read planted text <code class="font-mono text-[0.85em]">PROMPTBOX-7742</code> and all three shapes from a generated image. Run <code class="font-mono text-[0.85em]">python3 ~/promptbox/plugins/glm-vision/test/run_test.py</code> to verify your key.</span>
        <span data-lang="ko">실제 glm-4.6v API로 종단 테스트를 통과했다(2026-08-18) — 테스트 이미지에 심어둔 문구 <code class="font-mono text-[0.85em]">PROMPTBOX-7742</code>와 도형 3개를 정확히 읽었다. 내 키가 잘 작동하는지는 <code class="font-mono text-[0.85em]">python3 ~/promptbox/plugins/glm-vision/test/run_test.py</code>로 확인할 수 있다.</span>
      </p>
    </section>

    <section class="glass-panel rounded-2xl p-5 lg:p-6">
      <h2 class="text-lg font-semibold text-ink-900 dark:text-ink-50">
        <span data-lang="en">Why this setup?</span>
        <span data-lang="ko">왜 이 구성인가?</span>
      </h2>
      <p class="mt-2 text-sm leading-relaxed text-ink-600 dark:text-ink-300">
        <span data-lang="en">OpenCode ships no model of its own, so one Z.ai coding-plan key is the whole cost, and you can swap providers later without changing harness. oh-my-opencode-slim turns that single agent into a team whose members can each run a different model, so the expensive lane and the cheap lane are separate line items. GLM-5.3 carries a 1,000,000-token context, which is what makes "read the whole repo first" realistic.</span>
        <span data-lang="ko">OpenCode는 자체 모델이 없어서 Z.ai 코딩 플랜 키 하나가 비용의 전부이고, 나중에 하네스를 바꾸지 않고 모델만 갈아끼울 수 있다. oh-my-opencode-slim은 그 한 명을 팀으로 만들되 구성원마다 다른 모델을 물릴 수 있어서, 비싼 자리와 싼 자리를 분리해 쓸 수 있다. GLM-5.3의 컨텍스트는 100만 토큰이라 "저장소 전체를 먼저 읽고 시작하는" 방식이 현실적으로 가능해진다.</span>
      </p>
      <p class="mt-2 text-sm leading-relaxed text-ink-600 dark:text-ink-300">
        <a class="text-accent-700 dark:text-accent-400 font-medium underline underline-offset-2" href={`${base}/harnesses/opencode/`}>OpenCode 상세 →</a>
        <span class="mx-2 text-ink-400">·</span>
        <a class="text-accent-700 dark:text-accent-400 font-medium underline underline-offset-2" href={`${base}/harnesses/oh-my-opencode-slim/`}>oh-my-opencode-slim 상세 →</a>
      </p>
    </section>

    <!-- 대안 -->
    <section class="glass-panel rounded-2xl p-5 lg:p-6">
      <details class="group">
        <summary class="cursor-pointer list-none">
          <span class="text-lg font-semibold text-ink-900 dark:text-ink-50">
            <span data-lang="en">Alternative — Prime Agent (single-agent harness)</span>
            <span data-lang="ko">다른 선택지 — Prime Agent (에이전트 한 명짜리 하네스)</span>
          </span>
          <span class="ml-2 text-sm text-ink-500 group-open:hidden">▸</span>
          <span class="ml-2 hidden text-sm text-ink-500 group-open:inline">▾</span>
        </summary>
        <p class="mt-3 text-sm leading-relaxed text-ink-600 dark:text-ink-300">
          <span data-lang="en">If a team of seven feels like too much machinery, <a class="text-accent-700 dark:text-accent-400 font-medium underline underline-offset-2" href={`${base}/harnesses/prime-agent/`}>Prime Agent</a> is the opposite bet: one agent whose only tool is a persistent IPython kernel, so Python variables and loaded data survive across turns. It reads the same <code class="font-mono text-[0.85em]">~/.agents/skills/</code> directory this site installs into, and it takes the same Z.ai key.</span>
          <span data-lang="ko">일곱 명짜리 팀이 과하게 느껴진다면, <a class="text-accent-700 dark:text-accent-400 font-medium underline underline-offset-2" href={`${base}/harnesses/prime-agent/`}>Prime Agent</a>는 정반대 선택이다. 도구가 영구 IPython 커널(살아 있는 파이썬 환경) 하나뿐인 단일 에이전트라, 변수와 읽어둔 데이터가 턴이 바뀌어도 남아 있다. 이 사이트가 설치하는 <code class="font-mono text-[0.85em]">~/.agents/skills/</code>를 그대로 읽고, Z.ai 키도 같은 것을 쓴다.</span>
        </p>
        <div class="mt-3 flex items-stretch gap-2">
          <code id="tip-prime" class="min-w-0 flex-1 overflow-x-auto whitespace-pre rounded-lg border border-ink-800/70 bg-ink-950 px-3 py-2 font-mono text-[0.82rem] text-ink-100">{cmdPrimeInstall}</code>
          <CopyButton target="#tip-prime" labelEn="Copy" labelKo="복사" size="sm" variant="ghost" class="shrink-0" />
        </div>
        <p class="mt-3 text-sm leading-relaxed text-ink-600 dark:text-ink-300">
          <span data-lang="en">Needs Node.js 22.8+. Key storage is <code class="font-mono text-[0.85em]">~/.prime/agent/auth.json</code>; the env-var route is <code class="font-mono text-[0.85em]">export ZAI_API_KEY=…</code>. Unlike OpenCode it can take pasted screenshots via <kbd class="rounded border border-ink-300 px-1 font-mono text-xs dark:border-ink-700">Ctrl+V</kbd>, but only if the model you selected can read images — GLM Coding Plan models cannot.</span>
          <span data-lang="ko">Node.js 22.8 이상이 필요하다. 키는 <code class="font-mono text-[0.85em]">~/.prime/agent/auth.json</code>에 저장되고, 환경변수로 넣으려면 <code class="font-mono text-[0.85em]">export ZAI_API_KEY=…</code>. <kbd class="rounded border border-ink-300 px-1 font-mono text-xs dark:border-ink-700">Ctrl+V</kbd>로 스크린샷을 붙여넣는 기능이 있지만, 고른 모델이 이미지를 읽을 수 있어야 의미가 있다 — GLM Coding Plan 모델은 읽지 못한다.</span>
        </p>
      </details>
    </section>

  </div>
</BaseLayout>
